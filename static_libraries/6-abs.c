#include "main.h"
/**
 * _abs - computes the absolute value of an number
 * @n: the passed argument
 * Return: the absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-1 * n);
}
